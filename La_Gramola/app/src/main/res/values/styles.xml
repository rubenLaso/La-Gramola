<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green_scheme_colorPrimary</item>
        <item name="colorPrimaryDark">@color/green_scheme_colorPrimaryDark</item>
        <item name="colorAccent">@color/green_scheme_colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="BlueTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue_scheme_colorPrimary</item>
        <item name="colorPrimaryDark">@color/blue_scheme_colorPrimaryDark</item>
        <item name="colorAccent">@color/blue_scheme_colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="RedTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/red_scheme_colorPrimary</item>
        <item name="colorPrimaryDark">@color/red_scheme_colorPrimaryDark</item>
        <item name="colorAccent">@color/red_scheme_colorAccent</item>
    </style>

</resources>
